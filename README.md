Screenshots referenced from the main README. Filenames must match exactly
(case-sensitive on Linux/GitHub):

- `main-screen.png` — the startup banner (`python farsight.py -p "..."` or
  `--list-techniques`, just the top of the output)
- `example-usage.png` — a normal generation run showing a few colored,
  labeled payloads
- `list-techniques.png` — `python farsight.py --list-techniques` output,
  showing the category grouping and per-technique colors

PNG over JPG for terminal screenshots — text stays crisp, no compression
artifacts. This file can be deleted once real screenshots are added.
