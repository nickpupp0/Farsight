# Only set the keys for the provider(s) you plan to use.

# Required for --provider openai
OPENAI_API_KEY=

# Required for --provider claude
ANTHROPIC_API_KEY=

# Optional for --provider ollama (defaults to http://localhost:11434)
OLLAMA_HOST=http://localhost:11434
